let num1 = 31;  // First factor of 62
let num2 = 2;   // Second factor of 62
let multiply = num1 * num2;  // multiply = 62
const random = Math.floor(Math.random() * 100) + 1; // Random integer between 1 and 100
let num3 = 14;  // Dividend (e.g., 14 ÷ 5 = 2 with remainder 4)
let num4 = 5;   // Divisor
let mod = num3 % num4;  // mod = 4 (remainder)
const numbers = [10, 5, 20, 15]; // A set of numbers where 20 is the highest
const max = Math.max(...numbers); // max = 20